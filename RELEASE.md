# Release

Current release: Owlbox Alpha 0.1.3.

The canonical human-readable release name is `Owlbox Alpha 0.1.3`. Use `v0.1.3-alpha` for the matching Git tag and `Release Owlbox Alpha 0.1.3` for the release commit subject.

Release after reviewing the complete staged change set, rebuilding generated files, committing sources and generated outputs together, then creating and pushing the matching tag.

Do not create a separate changelog. Generated LEGACY records OWL-source changes, and Git commits and tags record repository releases.
